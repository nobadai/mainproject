"""calendar_walk.py — 달력을 **하루씩 걷는 상한** 하나. 두 모듈이 같이 쓴다.

마스터에는 달력을 하루씩 걷는 자리가 둘이다. 방향만 다르고 멈추는 이유는 같다.

```text
day_open        as_of 에서 **뒤로**  걸으며 마지막으로 열린 날을 찾는다
execution_day   day 에서   **앞으로** 걸으며 다음 실행일을 찾는다
```

🔴 **둘 다 상한이 없으면 조용히 망가진다.** 뒤로는 수백 행이 생기고, 앞으로는
   무한 반복이 된다.

★ **왜 31인가.** 번인이 30일이다. 그보다 크게 건너뛰는 것은 **의도보다 실수일
  가능성이 크다** — 원문은 `day_open.py` 의 `MAX_CARRY_DAYS` 주석에 있었고, 이
  파일은 그 이유를 옮겨 온 것이지 새로 정한 것이 아니다.

  앞으로 걷는 쪽에도 같은 수가 맞다. 우리나라에서 주말·공휴일이 이어지는 가장 긴
  구간이 닷새 남짓이므로(실측 2025-10-05~10-09 추석 연휴), **31일을 걸어도 실행일이
  안 나오면 그건 연휴가 아니라 달력이 틀린 것**이다. 그때는 걷기를 늘릴 게 아니라
  막고 사유를 내야 한다.

🔴 **왜 모듈을 따로 두었나 — 수를 복사해 두지 않기 위해서다.**

  ```text
  day_open 이 execution_day 를 임포트하면 안 된다
      tests/master/test_day_open.py 가 원문으로 막는다 ("import execution_day")
      하루 넘김이 실행일 달력을 쓰기 시작하면 토·일 이틀치 사실이 사라진다

  execution_day 가 day_open 을 임포트하면 안 된다
      day_open 은 `app.finance.db` 를 끌고 온다 — execution_day 는 순수해야 한다
      (매입이 이 함수를 인용할 예정이고, 매입은 DB 를 못 부른다)
  ```

  둘 다 못 부르면 남는 길은 **둘 다 부를 수 있는 빈 자리** 하나다. 이 파일에는
  임포트가 없다 — 그래야 순수한 쪽이 안전하게 가리킬 수 있다.
"""

from __future__ import annotations

__all__ = ["MAX_WALK_DAYS"]

#: 달력을 하루씩 걸을 때의 상한. **넘으면 막고 사유를 낸다** — 늘려서 지나가지 않는다.
MAX_WALK_DAYS = 31
