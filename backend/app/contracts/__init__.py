"""공용 계약 패키지.

한 파트의 소유가 아니다. 다섯 파트가 **대등하게** 참조한다.

전에는 `app/orchestrator/contracts_core.py` 에 있었고, 그래서 마스터·재무·물류·매입이
전부 *"오케스트레이터를 임포트하는"* 모양이었다. 실제로 쓰는 것은 계약 타입 둘셋뿐인데
폴더 이름이 의존 관계를 잘못 말했다.

★ **여기에 실행 코드를 두지 않는다.** 타입·어휘·계약 위반 예외까지다. 계산이 들어오면
  그 순간 다시 한 파트의 폴더가 된다.
"""
