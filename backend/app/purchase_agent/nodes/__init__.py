"""7노드 (상세설계 §4). 노드는 **바꿀 키만 담은 dict**를 반환하고 런타임이 병합한다.

Epic 2 골격이 ①③⑥⑦을 세웠고 ②④⑤는 통과 스텁이었다 (백로그 E2-1 "7노드 빈 껍데기").
Epic 3에서 셋 다 실제 로직으로 바뀌었다 — ⑤ 등급 배분(E3-1) · ④ 분할(E3-3) ·
② 문서 루프(E3-4). **일곱 노드가 전부 자기 일을 한다.**

**숫자는 전부 계산이 소유한다** (CLAUDE.md 규칙 6). 남은 LLM 몫은 각 노드에 주석으로
표시해 두었다 — ⑤의 조합 트레이드오프 판단(E3-2), ②의 문서 선택·충분성 판정·발췌 선별,
④의 회차별 수량·날짜 배분. 그때도 규칙이 만든 숫자를 LLM이 덮어쓰는 게 아니라
문장·조합 선택만 맡는다.
"""
