"""7노드 (상세설계 §4). 노드는 **바꿀 키만 담은 dict**를 반환하고 런타임이 병합한다.

Epic 2 골격에서 구현한 것은 ①③⑥⑦이고 ②④⑤는 통과 스텁이다 (백로그 E2-1
"7노드 빈 껍데기", §11 구현 순서 3단계). 스텁도 자리를 채우는 이유는 ⑦의 사중 일치가
``split_plan``·``sourcing_plan`` 없이는 성립하지 않기 때문이다.

**숫자는 전부 계산이 소유한다** (CLAUDE.md 규칙 6). 각 노드의 LLM 몫은 주석으로 표시하고
Epic 3에서 붙인다 — 그때도 규칙이 만든 숫자를 LLM이 덮어쓰는 게 아니라, 문장·조합 선택만
맡는다.
"""
